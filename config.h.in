#pragma once

namespace Config
{
  inline constexpr const char* CalibrationFile =
    "@CALIBRATION_FILE@";
  
  inline constexpr const char* NormalizationFile =
    "@NORMALIZATION_FILE@";

  inline constexpr const char* RootOutputFile =
    "@ROOT_OUTPUT_FILE@";

  inline constexpr const char* AnaDirectory =
    "@ANALYSIS_DATA_DIRECTORY@";

  inline constexpr const char* BaseFolder =
    "@BASE_DATA_DIRECTORY@";
  
  inline constexpr const int BookStatus =
    @BOOK_STATUS@;
  // Some Analysis Variables No Need to Change
  inline constexpr double CutoffWavelengths[2] ={
    @CUTOFF_WAVELENGTHS_MIN@,
    @CUTOFF_WAVELENGTHS_MAX@
  };
  inline constexpr int NBkgFiles =
    @N_BKGFILES@;

  inline constexpr int NDataFiles =
    @N_DATAFILES@;
}
